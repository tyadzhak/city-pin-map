// PNG export via dom-to-image-more (loaded as a global from index.html CDN).
// Library choice is fixed in index.html — do not switch to html-to-image.
// Filled in by CORE-012.

export async function exportMapPng() {}
