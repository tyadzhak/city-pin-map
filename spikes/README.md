# spikes/

Throwaway prototypes that informed past tasks. Safe to delete a prototype once its parent task is closed and any follow-ups it triggered are filed.

These are **not** wired into the main app. They run as standalone HTML pages from `python3 -m http.server` (or any static server) at the repo root.

## Index

- `harden-008-maplibre/` — MapLibre GL JS + OpenFreeMap evaluation. Verdict: **PARK**. See `jira/harden/HARDEN-008-findings.md` for the full reasoning before pitching a vector-tile rewrite.
